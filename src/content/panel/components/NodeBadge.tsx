// Branch indicator badge — shown only when branchTotal > 1.

import React from 'react';

interface Props {
  branchCurrent: number;
  branchTotal: number;
}

export function NodeBadge(_props: Props): React.ReactElement | null {
  // TODO: implement — return null when branchTotal <= 1
  return null;
}
