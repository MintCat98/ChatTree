// Panel header — displays title and collapse/expand toggle.

import React from 'react';

interface Props {
  // TODO: define props (title, onToggle)
}

export function Header(_props: Props): React.ReactElement {
  // TODO: implement
  return <div />;
}
