// Hover tooltip displaying the full original prompt text.

import React from 'react';

interface Props {
  text: string;
  // TODO: define additional props (x, y, visible)
}

export function Tooltip(_props: Props): React.ReactElement {
  // TODO: implement
  return <div />;
}
