// Outermost panel wrapper — handles positioning, opacity, and visibility.

import React from 'react';

interface Props {
  // TODO: define props (position, opacity, visible, children)
}

export function PanelShell(_props: Props): React.ReactElement {
  // TODO: implement
  return <div />;
}
