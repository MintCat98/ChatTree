// SVG line connecting a parent node to a child node.

import React from 'react';

interface Props {
  // TODO: define props (x1, y1, x2, y2, isActive)
}

export function TreeEdge(_props: Props): React.ReactElement {
  // TODO: implement
  return <line />;
}
