// D3-rendered interactive map shell
// Placeholder for now — real D3 board comes in the next step.

export function InteractiveMap() {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        width: '100%',
        height: '100%',
        color: 'var(--nav-color-text-muted)',
        fontFamily: 'var(--nav-font-family)',
        fontSize: 'var(--nav-font-size-sm)',
      }}
    >
      InteractiveMap works
    </div>
  );
}