// Unit tests for branch detection snapshot diffing logic.

import { takeSnapshot, detectBranch } from '@content/branch-detector';

describe('branch-detector', () => {
  describe('takeSnapshot', () => {
    it.todo('captures current chatbox DOM order as an array of IDs');
  });

  describe('detectBranch', () => {
    it.todo('returns null when no branch change occurred');
    it.todo('returns navId of the divergence point on branch switch');
  });
});
