// Unit tests for the MutationObserver-based chatbox watcher.

import { startObserving, stopObserving } from '@content/observer';

describe('observer', () => {
  it.todo('startObserving attaches a MutationObserver to the chat container');
  it.todo('stopObserving disconnects the observer');
  it.todo('observer fires callback when a new chatbox element is added');
});
