// Unit tests for Tracker — ID assignment and node list management.

import { Tracker } from '@content/tracker';

describe('Tracker', () => {
  it.todo('assigns sequential IDs starting from chatbox-0');
  it.todo('extracts prompt text from element');
  it.todo('reset clears the node list');
  it.todo('getNodes returns a copy, not a reference');
});
